;; test.angeh
;; Validation suite guaranteeing the Architectonic parser absolutely bypasses invalid syntax cascades securely natively!

(import lexer)
(import parser)
(import semantic)
(import compiler)

;; Structurally Malformed Code String testing the Error-Recovery Protocols natively
(def bad-source "(def good 10)
  ) ;; <-- Syntax Structural Fault [1]
  (def bad-paren (list 1 2 
  ;; <-- Missing Closing Parens structural sync fault [2]
")

(print "\n==============================================")
(print ">> [OS Test] Launching Native Compiler Stability Suite")
(print "==============================================\n")

;; Execute the native build-pipeline evaluating structural resilience against corrupt sources
(run-compiler-pipeline bad-source "fault-sim.angeh")

(print "\n>> [OS Test] Matrix Execution Sequence Maintained. System didn't halt!")
